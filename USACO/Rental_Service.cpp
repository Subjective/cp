#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int n, t;
int main(void) {
    cin.tie(0)->sync_with_stdio(0);

    cin >> n >> t;
    int books[n];
    for (int i = 0; i < n; i++) {

    }
}